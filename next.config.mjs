/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["dgfradkmlelexoaayqfg.supabase.co", "images.unsplash.com", "picsum.photos"],
  },
};

export default nextConfig;
